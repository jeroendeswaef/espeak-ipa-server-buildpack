#!/bin/sh

indent() {
  sed -u 's/^/       /'
}

echo "-----> Found an file app-to-run"

# if hello.txt is empty, abort the build
if [ ! -s $1/app-to-run ]; then
  echo "app-to-run was empty" | indent
  exit 1
fi